name: Debug OIDC

# Trigger the workflow on push to only this file on main branch
on:
  push:
    paths:
      - '.github/workflows/oidc/**'
    branches:
      - main

jobs:
  oidc_debug_test:
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    name: oidc debugger
    steps:
      - name: Checkout actions-oidc-debugger
        uses: actions/checkout@v3
        with:
          repository: github/actions-oidc-debugger
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}
          path: ./.github/actions/actions-oidc-debugger
      - name: Debug OIDC Claims
        uses: ./.github/actions/actions-oidc-debugger
        with:
          audience: 'projects/gh-actions-1506/locations/global/workloadIdentityPools/gh-actions-1506/providers/github-actions-pool'
          # '${{ github.server_url }}/${{ github.repository_owner }}'